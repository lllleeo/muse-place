import Scroll from "./Scroll";

const ScrollArray = [];

const Scrolls = () => {
  return (
    <group>
      <Scroll text="Hello World" position={[15, 5, 30]} />
    </group>
  );
};

export default Scrolls;
